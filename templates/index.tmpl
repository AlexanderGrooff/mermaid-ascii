<html>
<head>
    <style>
        .container {
            display: flex;
        }
        .column {
            flex: 50%;
            padding: 10px;
        }
        textarea {
            width: 100%;
            height: 300px;
        }
    </style>
    <script src="https://unpkg.com/htmx.org@1.7.0"></script>
</head>
<body>
    <h1>Mermaid ASCII</h1>
    <div class="container">
        <div class="column">
            <p>Note: styleclasses are not yet supported</p>
            <form hx-post="/generate" hx-target=".result">
                <textarea name="inputText" placeholder="graph LR"></textarea>
                <button type="submit">Generate</button>
            </form>
        </div>
        <div class="column result">
            <!-- Result content goes here -->
        </div>
    </div>
</body>
</html>
