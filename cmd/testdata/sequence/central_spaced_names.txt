sequenceDiagram
    cron job ()->>() data svc: run
---
┌──────────┐     ┌──────────┐
│ cron job │     │ data svc │
└─────┬────┘     └─────┬────┘
      │                │
      │ run            │
      o───────────────►o
      │                │
